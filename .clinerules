When debugging or running python code, always activate the virtual environment first:
. ./.venv/bin/activate

When creating or modifying Python files, use ruff for checking and formatting:
uv run ruff format <file_path>
uv run ruff check --fix <file_path>
